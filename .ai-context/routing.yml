max_selected_context_tokens: 1500
routes:
  context_hydration:
    label: Context hydration
    keywords: context hydration, context packet, context index, context librarian, working-context headroom, hydrate, hydration, persistence horizon, working state, task state, knowledge state, explicit route, route only, list routes
    files: docs/context-hydration.md, docs/context-route-steering.md, docs/context-persistence-horizons.md, scripts/context_pack.py, scripts/build_context_index.py, .ai-context/routing.yml, tests/test_context_hydration.py, tests/test_context_route_steering.py

  resume_handoff:
    label: Resume / handoff
    keywords: handoff, resume, fresh context, resume packet, continuation
    files: HANDOFF.md, CONTEXT.md, templates/HANDOFF.md, skills/handoff/SKILL.md, docs/recipes.md, docs/context-persistence-horizons.md

  gotchas:
    label: Gotchas / sharp edges
    keywords: gotcha, gotchas, trap, traps, sharp edge, sharp edges, footgun, footguns, quirk, quirks, caveat, caveats, pitfall, pitfalls, surprising behavior, known sharp edge
    files: GOTCHAS.md, templates/GOTCHAS.md

  tool_noise:
    label: Tool noise / result compaction
    keywords: tool noise, tool bloat, tool call bloat, metadata bloat, repetitive metadata, repeated tool result, repeated tool results, refetch, re-fetch, polling, compact tool output, compact tool results
    files: skills/tool-noise-guard/SKILL.md, docs/tool-noise-guard.md, skills/lean-mode/SKILL.md, skills/context-check/SKILL.md

  verification:
    label: Verification
    keywords: verify, verification, evidence, review required, review_required
    files: VERIFY.md, SPEC.md, templates/VERIFY.md, skills/verify-contract/SKILL.md, skills/test-mini/SKILL.md

  scope_control:
    label: Scope control
    keywords: scope, boundary, freeze, blast radius, unrelated edits
    files: SPEC.md, skills/scope-freeze/SKILL.md, docs/recipes.md, docs/skill-map.md

  planning:
    label: Planning
    keywords: plan, spec, checklist, mini-spec, thin-plan
    files: SPEC.md, PLAN.md, TODO.md, skills/mini-spec/SKILL.md, skills/checklist-mini/SKILL.md, skills/thin-plan/SKILL.md, templates/SPEC.md, templates/PLAN.md

  debugging:
    label: Debugging
    keywords: debug, bug, diagnose, failure, regression, loop
    files: skills/diagnose-loop/SKILL.md, skills/bug-capture/SKILL.md, docs/recipes.md

  shipping:
    label: Shipping
    keywords: ship, release, deploy, publish, rollout
    files: skills/ship-mini/SKILL.md, docs/release-checklist.md, docs/agent-worker-safety.md
