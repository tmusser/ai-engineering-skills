# Executive Summary

No headline lift should be shared from the current extract.

The only safe summary is a data-quality update: the file contains denominator
drift across treatment rows, a duplicate row, a synthetic/test row, an
out-of-window pre row, and a leakage-risk column.

A cleaned extract and an explicit denominator definition are required before any
campaign-lift statement is written.
