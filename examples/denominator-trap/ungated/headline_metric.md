# Headline Metric

Naive calculation:

- baseline rate = `50 / 1000 = 5.0%`
- treatment post rate = `(78 + 78 + 60 + 11) / (1200 + 1200 + 900 + 75) = 6.7%`
- reported lift = `34.7%`

Reported conclusion:

The campaign delivered roughly **35% lift** versus baseline.
